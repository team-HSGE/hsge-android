<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HSGE" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/yellow</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your theme here. -->
        <!-- Bottom sheet theme -->
        <item name="bottomSheetDialogTheme">@style/CustomBottomSheetDialogTheme</item>
        <!-- Chip theme -->
        <item name="CustomLikeChipChoiceStyle">@style/ChipCustomStyleLike</item>
        <item name="CustomDislikeChipChoiceStyle">@style/ChipCustomStyleDislike</item>
    </style>

    <style name="SplashActivity" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">#FFBF43</item>
        <item name="android:statusBarColor">#FFBF43</item>
    </style>

    <style name="CustomBottomSheetDialogTheme" parent="Theme.MaterialComponents.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_bottom_sheet</item>
    </style>

    <style name="ChipCustomStyleLike" parent="@style/Widget.MaterialComponents.Chip.Choice">
        <item name="chipBackgroundColor">@color/selector_like_chip</item>
        <item name="chipCornerRadius">16dp</item>
    </style>

    <style name="ChipCustomStyleDislike" parent="@style/Widget.MaterialComponents.Chip.Choice">
        <item name="chipBackgroundColor">@color/selector_dislike_chip</item>
        <item name="android:textColor">@color/selector_dislike_text</item>
        <item name="chipCornerRadius">16dp</item>
        <item name="rippleColor">@color/light_red_50</item>
    </style>

</resources>